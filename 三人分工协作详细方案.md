# 三人团队分工协作详细方案

> **对应项目**：Hermes Agent 智能学习规划助手（如意学习规划助手）
> **版本**：v1.1（实际完成版） | **日期**：2026-07-23
> **前置文档**：`HermesAgent-学习规划助手-设计方案.md`

---

## 目录

- [一、角色定义与职责边界](#一角色定义与职责边界)
- [二、实际完成情况](#二实际完成情况)
- [三、文件级所有权清单](#三文件级所有权清单)
- [四、依赖关系与交接协议](#四依赖关系与交接协议)
- [五、Git 工作流](#五git工作流)
- [六、沟通与同步机制](#六沟通与同步机制)
- [七、阻塞处理清单](#七阻塞处理清单)
- [八、验收标准汇总](#八验收标准汇总)

---

## 一、角色定义与职责边界

### 角色总览

```
┌─────────────────────────────────────────────────────────┐
│                    三人分工矩阵                           │
│                                                         │
│  ┌─────────────────┐  ┌─────────────────┐  ┌──────────┐│
│  │   A：后端+Agent   │  │   B：前端工程师   │  │ C：项目整合+质量│
│  │                  │  │                  │  │   保障     ││
│  │  Hermes Agent    │  │  React 页面      │  │ 项目整合  │
│  │  Skill/Tool 开发  │  │  对话组件        │  │ 测试执行  │
│  │  FastAPI 传输层   │  │  计划看板        │  │ Bug修复   │
│  │  LLM Prompt      │  │  进度页面        │  │ 知识库文档│
│  │  用户认证系统     │  │  认证状态管理     │  │ 文档编写  │
│  │  权限校验         │  │  UI/UX 设计      │  │ 安全审计  │
│  └────────┬─────────┘  └────────┬─────────┘  └────┬─────┘│
│           │                     │                  │      │
│           └──────────┬──────────┴─────────┬────────┘      │
│                      │                   │                │
│               API 契约对接          知识库数据供给          │
└─────────────────────────────────────────────────────────┘
```

### 角色 A：后端 + Agent 集成工程师

| 维度 | 说明 |
|------|------|
| **核心职责** | Hermes Agent skill 开发、LLM Prompt 工程、FastAPI 传输层、SSE 流式通信、用户认证系统（注册/登录/会话管理）、权限校验、安全修复（路径遍历/权限校验/空消息）、自适应调整引擎 |
| **所需技能** | Python、Hermes Agent CLI/API、LLM Prompt Engineering、FastAPI、Cookie 认证、路径安全、SQLite |
| **产出物** | skill 定义文件、Python 工具函数、FastAPI 路由、认证中间件、权限校验、Prompt 模板、数据存取层、自适应调整引擎 |
| **实际完成** | ✅ 全部完成（含所有安全修复和认证功能） |

### 角色 B：前端工程师

| 维度 | 说明 |
|------|------|
| **核心职责** | React 页面开发（登录/对话/计划/今日任务）、对话组件、计划看板、进度页面、API 对接、认证状态管理（useAuth + AuthContext）、UI/UX、日历导出、会话管理（暂停/清除）、用户数据隔离 |
| **所需技能** | React + TypeScript、Tailwind CSS、SSE 流式对接、Vite、Context 状态管理、AbortController |
| **产出物** | 4 个页面组件、对话组件、路由配置（含 ProtectedRoute）、API 调用封装（含下载 API）、认证状态管理、会话管理功能 |
| **实际完成** | ✅ 全部完成（含用户数据隔离和会话管理功能） |

### 角色 C：项目整合 + 质量保障工程师

| 维度 | 说明 |
|------|------|
| **核心职责** | 项目整体整合（前后端联调、环境配置）、测试用例设计与执行（API测试、端到端测试）、问题发现与定位（Bug排查、错误日志分析）、Bug修复与回归验证（安全漏洞修复、功能缺陷修复）、知识库文档编写与上传、项目文档撰写（架构/API/部署/分工方案/设计方案）、演示脚本准备、安全审计 |
| **所需技能** | Markdown、Dify Web 界面操作、基础 Python、文档写作、安全审计、测试、问题排查与定位、前后端联调 |
| **产出物** | 结构化知识库 .md 文件（13章）、练习题（13套）、测试报告、项目文档、安全修复、整合测试报告、Bug修复记录 |
| **实际完成** | ✅ 全部完成（含项目整合、测试、Bug修复、安全审计和所有文档更新） |

---

## 二、实际完成情况

### 实际完成的功能清单

#### 角色 A（后端+Agent）完成的功能

| 功能模块 | 完成情况 | 说明 |
|----------|:-------:|------|
| FastAPI 项目骨架 | ✅ | `api/app.py`、路由注册、中间件配置 |
| Dify 客户端封装 | ✅ | `src/dify_client.py`，支持 RAG 检索 |
| 用户画像管理 | ✅ | `src/profile_manager.py`，JSON 文件 CRUD |
| 进度追踪模块 | ✅ | `src/agent/session_db.py` + SQLite |
| Hermes Agent 集成 | ✅ | `src/llm/hermes_client.py`，支持 HTTP 连接 |
| 工具函数开发 | ✅ | call_llm、manage_profile、retrieve_knowledge、evaluate_plan、generate_schedule |
| Prompt 工程 | ✅ | plan_generation、plan_adjustment、profile_collection |
| SSE 流式聊天端点 | ✅ | `/api/v1/learn/chat`，支持 token 级推送 |
| 计划管理端点 | ✅ | 获取计划、调整计划、导出日历 |
| 会话管理端点 | ✅ | 会话列表、消息历史、删除会话 |
| **用户认证系统** | ✅ | `src/auth.py`，用户名密码注册/登录/会话管理 |
| **认证中间件** | ✅ | `api/middlewares/auth.py`，权限校验、豁免路径 |
| **权限校验** | ✅ | 所有 API 验证用户只能访问自己的数据 |
| **路径安全** | ✅ | `src/utils/path_security.py`，防路径遍历攻击 |
| **消息即时存储** | ✅ | 生成过程中定时保存到 SQLite |
| **自适应调整** | ✅ | `src/learner/plan_adjuster.py`，任务顺延/任务量调整/难度自适应 |

#### 角色 B（前端）完成的功能

| 功能模块 | 完成情况 | 说明 |
|----------|:-------:|------|
| React 项目骨架 | ✅ | Vite + Tailwind + TypeScript |
| 路由配置 | ✅ | `/learn`、`/learn/plan/:id`、`/learn/today`、`/login` |
| Layout 框架 | ✅ | Header + Sidebar + 主内容区，深色主题 |
| 对话组件 | ✅ | `LearnChatPage.tsx`，支持 SSE 流式、Markdown 渲染 |
| 计划看板 | ✅ | `PlanViewPage.tsx`，卡片式周计划 + 每日任务展开 |
| 今日任务 | ✅ | `TodayPage.tsx`，任务清单 + 打卡 + 进度统计 |
| **登录页面** | ✅ | `LoginPage.tsx`，登录/注册切换，表单验证 |
| **认证状态管理** | ✅ | `useAuth.ts` + `AuthContext.tsx`，全局状态共享 |
| **路由守卫** | ✅ | `ProtectedRoute`，未登录自动跳转登录页 |
| **日历导出** | ✅ | `apiDownloadICS()`，下载 .ics 文件 |
| **清除对话** | ✅ | 调用后端 API 删除会话 |
| **暂停生成** | ✅ | AbortController 中断 SSE 连接 |
| **用户数据隔离** | ✅ | localStorage key 按用户区分 |

#### 角色 C（项目整合+质量保障）完成的功能

| 功能模块 | 完成情况 | 说明 |
|----------|:-------:|------|
| **项目整合** | ✅ | 前后端联调、环境配置、依赖安装、启动脚本配置 |
| **测试用例设计与执行** | ✅ | API 测试套件、端到端测试、功能测试 |
| **问题发现与定位** | ✅ | Bug排查、错误日志分析、控制台报错定位 |
| **Bug修复与回归验证** | ✅ | 安全漏洞修复、功能缺陷修复、修复后回归测试 |
| **安全问题修复** | ✅ | doc_id 白名单校验、plan_id 归属校验、路径安全加固 |
| Python 学习路径文档 | ✅ | 13 章结构化 Markdown，含元数据、前置知识标注 |
| 练习题文档 | ✅ | 13 套练习题，含题目+提示+参考解答 |
| Dify 知识库上传 | ✅ | 知识库文档已上传到 Dify |
| 本地知识库管理 | ✅ | `src/knowledge_base_manager.py`，资源匹配引擎 |
| 项目文档 | ✅ | ARCHITECTURE.md、README.md、三人分工方案、设计方案 |
| 演示准备 | ✅ | 演示脚本、环境配置、演示用例预演 |

### 实际完成的 API 端点

| 端点 | 方法 | 所属模块 | 开发人 |
|------|------|---------|:-----:|
| `/auth/login` | POST | 用户认证 | A |
| `/auth/register` | POST | 用户认证 | A |
| `/auth/logout` | POST | 用户认证 | A |
| `/auth/status` | GET | 用户认证 | A |
| `/learn/chat` | POST | 学习对话 | A |
| `/learn/plan/{id}` | GET | 计划管理 | A |
| `/learn/plan/{id}/ics` | GET | 日历导出 | A |
| `/learn/plan/{id}/adjust` | POST | 计划调整 | A |
| `/learn/session/list` | GET | 会话管理 | A |
| `/learn/session/{id}` | GET/DELETE | 会话管理 | A |
| `/learn/session/{id}/messages` | GET | 会话管理 | A |
| `/profile/{id}` | GET/POST | 用户画像 | A |
| `/profile/{id}/init` | POST | 用户画像 | A |
| `/progress/checkin` | POST | 进度追踪 | A |
| `/progress/stats/{id}` | GET | 进度统计 | A |
| `/progress/checkin/today/{id}` | GET | 今日打卡 | A |
| `/knowledge/list` | GET | 知识库 | A |
| `/knowledge/search` | GET | 知识库 | A |
| `/knowledge/doc/{id}` | GET | 知识库 | A |

---

## 三、文件级所有权清单

### A 负责文件

```
D:\ai\RuyiLearningPlanner\
├── server.py                        # uvicorn 启动入口
├── api/
│   ├── app.py                       # FastAPI 应用 + CORS + 路由注册
│   ├── deps.py                      # 依赖注入（获取当前用户）
│   ├── middlewares/
│   │   └── auth.py                  # 认证中间件
│   └── v1/endpoints/
│       ├── __init__.py
│       ├── auth.py                  # 用户认证（登录/注册/登出）
│       ├── learn_chat.py            # SSE 流式聊天端点
│       ├── profile.py               # 画像 CRUD
│       ├── progress.py              # 进度 CRUD + 自适应调整
│       └── knowledge.py             # 知识库文档
├── src/
│   ├── agent/
│   │   ├── orchestrator.py          # 流程调度器
│   │   ├── tool_registry.py         # 工具注册表
│   │   └── session_db.py            # 会话持久化（SQLite）
│   ├── llm/
│   │   └── hermes_client.py         # Hermes Agent HTTP 客户端
│   ├── learner/
│   │   ├── models.py                # 数据模型
│   │   ├── plan_engine.py           # 计划生成引擎
│   │   ├── plan_adjuster.py         # 计划自适应调整器
│   │   └── prompts/                 # 提示词模板
│   ├── dify_client.py               # Dify KB 检索客户端
│   ├── knowledge_base_manager.py    # 本地知识库管理
│   ├── profile_manager.py           # 画像管理
│   ├── prerequisite_checker.py      # 前置依赖检查器
│   ├── auth.py                      # 用户认证核心
│   ├── config.py                    # 配置管理
│   └── utils/
│       └── path_security.py         # 路径安全工具
├── tools/
│   ├── call_llm.py
│   ├── manage_profile.py
│   ├── retrieve_knowledge.py
│   ├── evaluate_plan.py
│   └── generate_schedule.py
└── skills/
    └── learning-planner/
        ├── SKILL.md
        ├── tools/
        └── prompts/
```

### B 负责文件

```
D:\ai\RuyiLearningPlanner\apps\learning-web\
├── index.html
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── src/
    ├── main.tsx                     # 入口（含 AuthProvider）
    ├── App.tsx                      # 路由配置（含 ProtectedRoute）
    ├── contexts/
    │   └── AuthContext.tsx          # 全局认证状态
    ├── pages/
    │   ├── LoginPage.tsx            # 登录/注册页面
    │   ├── LearnChatPage.tsx        # 对话首页
    │   ├── PlanViewPage.tsx         # 计划看板（含日历下载）
    │   └── TodayPage.tsx            # 今日任务 + 打卡
    ├── components/
    │   ├── learn/
    │   │   ├── LearningChat.tsx     # 对话组件
    │   │   ├── ChatMessage.tsx      # 单条消息
    │   │   ├── PlanOverview.tsx     # 计划看板
    │   │   ├── TaskList.tsx         # 任务清单
    │   │   └── FeedbackForm.tsx     # 反馈表单
    │   └── layout/
    │       ├── Header.tsx           # 顶部导航（含用户信息）
    │       ├── Sidebar.tsx          # 侧边栏
    │       └── Layout.tsx           # 布局容器
    ├── hooks/
    │   ├── useAuth.ts               # 认证状态管理
    │   ├── useLearningChat.ts       # 对话状态 + SSE
    │   └── usePlan.ts               # 计划数据
    ├── services/
    │   └── api.ts                   # HTTP 封装（含下载 API）
    ├── store/
    │   └── appStore.tsx             # 应用状态管理（按用户隔离）
    └── types/
        └── index.ts                 # TS 类型定义
```

### C 负责文件

```
D:\ai\RuyiLearningPlanner\
├── kb_docs/                         # 知识库源文件
│   ├── python_learning_path/        # Python 学习路径（13章）
│   │   ├── 01-基础语法.md
│   │   ├── 02-变量与数据类型.md
│   │   └── ...
│   └── exercises/                   # 练习题（13套）
│       ├── 01-基础语法练习题.md
│       └── ...
├── tests/                           # 测试用例
│   ├── test_dify_client.py          # Dify 客户端测试
│   ├── test_profile_manager.py      # 用户画像管理测试
│   ├── test_progress_tracker.py     # 进度追踪测试
│   ├── test_api_learn.py            # 学习对话 API 测试
│   ├── test_auth.py                 # 认证系统测试
│   ├── test_integration.py          # 前后端集成测试
│   └── test_security.py             # 安全漏洞测试
├── docs/                            # 文档
│   ├── architecture.md              # 架构文档
│   ├── api_spec.md                  # API 接口文档
│   ├── deployment.md                # 部署说明
│   ├── integration_test_report.md   # 集成测试报告
│   └── bug_fix_log.md               # Bug 修复记录
├── .env.example                     # 环境变量模板
├── README.md                        # 项目说明
├── ARCHITECTURE.md                  # 架构文档
├── 三人分工协作详细方案.md           # 分工方案
├── HermesAgent-学习规划助手-设计方案.md # 设计方案
└── .gitignore                       # Git 忽略规则
```

---

## 四、依赖关系与交接协议

### 4.1 依赖关系图

```
C 准备 Dify 知识库 ──────→ A 开发 Dify 客户端 ────→ A 开发 Hermes Skill
                                │                          │
                                │                    A 提供 API 契约
                                │                          │
                                ▼                          ▼
                           A 开发 FastAPI + Auth      B 开发前端页面 + Auth
                                │                          │
                                └──────────┬───────────────┘
                                           ▼
                                    **C 负责前后端联调整合**
                                           │
                                           ▼
                                    **C 执行端到端测试 + 安全审计**
                                           │
                                           ▼
                                    **C 发现问题并定位 → 协调修复**
                                           │
                                           ▼
                                    C 回归验证 → 全员确认
```

### 4.2 关键交接点

#### 交接点 1：C → A（已完成）

| 交接内容 | 格式 | 说明 |
|---------|------|------|
| Dify KB ID | 字符串 | 知识库的唯一 ID |
| Dify API Key | 字符串 | 已配置到 .env |
| Dify Base URL | URL | `http://localhost/v1` |
| 知识库文档 | Markdown 文件 | 13 章学习路径 + 13 套练习题 |

#### 交接点 2：A → B（已完成）

| 交接内容 | 格式 | 说明 |
|---------|------|------|
| SSE 端点格式文档 | Markdown | 事件类型和 data 格式 |
| Plan JSON Schema | TS Type | 计划返回的完整 JSON 结构 |
| 认证接口 | API 文档 | 登录/注册/登出/状态 |

#### 交接点 3：C → A + B（持续进行）

| 交接内容 | 说明 |
|---------|------|
| Bug List | 每次测试发现的 Bug，包含问题描述、复现步骤、影响范围 |
| 安全问题 | 路径遍历、权限校验等安全漏洞，附漏洞分析报告 |
| 测试反馈 | LLM 输出质量、UI 显示问题、性能问题 |
| 问题定位 | 协助定位问题根源，提供错误日志分析 |
| 修复方案建议 | 针对发现的问题提出修复方案建议 |

#### 交接点 4：A + B → C（持续进行）

| 交接内容 | 说明 |
|---------|------|
| 代码变更通知 | A/B 完成功能开发后通知 C 进行测试 |
| 修复完成通知 | A/B 修复 Bug 后通知 C 进行回归验证 |
| API 变更文档 | A 修改 API 后提供变更说明文档 |

---

## 五、Git 工作流

### 5.1 分支策略

```
main (保护分支 — 不可直接推送)
  │
  └── dev (日常集成分支)
        ├── feat/backend-agent    ← A 的工作分支
        ├── feat/frontend-ui      ← B 的工作分支
        └── feat/kb-content       ← C 的工作分支
```

### 5.2 Commit Message 规范

```bash
# 格式: type(scope): description
# 示例:
feat(api): 添加用户认证接口
feat(auth): 实现权限校验中间件
feat(ui): 完成登录页面
feat(kb): 上传 13 个 Python 学习章节
fix(security): 修复路径遍历漏洞
fix(auth): 修复登录状态不同步
test(api): 添加学习规划 API 测试用例
docs: 补充架构文档和部署说明
```

---

## 六、沟通与同步机制

### 6.1 每日站会（15 分钟）

| 轮次 | 发言者 | 内容 |
|:----:|:------:|------|
| 1 | A | 昨天做了什么、今天计划做什么、有什么阻塞 |
| 2 | B | 同上 |
| 3 | C | 同上，额外报告测试结论和安全问题 |
| 4 | 全员 | 确认当天的关键交接点 |

### 6.2 沟通渠道

| 场景 | 渠道 | 说明 |
|------|:----:|------|
| 每日站会 | 微信语音/腾讯会议 | 15 分钟同步 |
| 日常文字沟通 | 微信群 | 随时提问、发截图、报 Bug |
| 代码审查 | GitHub PR | 合并前在 PR 中 review |
| API 契约变更 | 微信群 @相关人 | A 改接口必须群内通知 B |
| Bug 报告 | GitHub Issues | C 创建 Issue，标注优先级 |

---

## 七、阻塞处理清单

### 已解决的阻塞

| # | 阻塞场景 | 责任方 | 解决方案 | 状态 |
|:-:|---------|:------:|---------|:----:|
| 1 | 认证中间件拦截注册接口 | A | 添加 `/api/v1/auth/register` 到豁免列表 | ✅ |
| 2 | 前端硬编码 demo_user | B | 创建 `useAuth` hook，替换所有硬编码 | ✅ |
| 3 | SSE 请求不带 Cookie | B | 添加 `credentials: 'include'` | ✅ |
| 4 | 对话消息不同步 | A | 实时保存消息到后端数据库（每 0.5s/50 token） | ✅ |
| 5 | 清除对话后重新出现 | A+B | 前端调用后端 DELETE API 删除会话，前后端同步删除 | ✅ |
| 6 | 空消息残留 | A | 收到第一个 token 时才创建 assistant 消息 | ✅ |
| 7 | 打卡 plan_id 归属校验 | A | 添加 `_plan_belongs_to_user()` 校验，验证 plan_id 属于当前用户 | ✅ |
| 8 | 知识库 doc_id 路径风险 | C | 添加白名单校验（只允许中文、字母、数字、下划线、连字符、点） | ✅ |
| 9 | useEffect 无限循环 | B | 移除不稳定的 setter 依赖，添加 cancelled 标志防止组件卸载后更新 | ✅ |
| 10 | 用户数据隔离问题 | B | localStorage key 按用户区分，不同用户数据独立存储 | ✅ |

### 安全修复清单

| # | 安全问题 | 修复方式 | 文件 |
|:-:|---------|---------|------|
| 1 | 路径遍历漏洞（plan_id） | 白名单验证 + os.path.basename() 规范化 | `src/utils/path_security.py` |
| 2 | 路径遍历漏洞（doc_id） | 白名单验证 + safe_doc_id() 校验 | `src/utils/path_security.py`, `knowledge.py` |
| 3 | 越权访问（plan_id 归属） | 添加 `_plan_belongs_to_user()` 校验 | `progress.py` |
| 4 | 越权访问（用户数据） | 所有 API 验证 user_id 匹配 | 所有 API 端点 |
| 5 | 空消息残留 | 收到第一个 token 时才创建消息 | `learn_chat.py` |
| 6 | 清除对话不彻底 | 前端调用后端 DELETE API 删除会话 | `useLearningChat.ts`, `api.ts` |

---

## 八、验收标准汇总

### 8.1 最终交付物清单

| # | 交付物 | 责任人 | 状态 |
|:-:|-------|:------:|:----:|
| 1 | 可运行的 FastAPI 后端服务 | A | ✅ |
| 2 | Hermes Agent learning-planner skill | A | ✅ |
| 3 | 用户认证系统（注册/登录/权限校验） | A | ✅ |
| 4 | 学习规划 API（对话/计划/进度/知识库） | A | ✅ |
| 5 | 自适应调整引擎（任务顺延/任务量调整/难度自适应） | A | ✅ |
| 6 | 安全修复（路径遍历/权限校验/空消息） | A+C | ✅ |
| 7 | 4 个前端页面（登录/对话/计划/今日任务） | B | ✅ |
| 8 | 认证状态管理（useAuth + AuthContext） | B | ✅ |
| 9 | 日历导出功能 | B | ✅ |
| 10 | Dify 知识库（Python 学习路径 13 章 + 练习题 13 套） | C | ✅ |
| 11 | API 端到端测试套件 | C | ✅ |
| 12 | 项目文档（架构/API/部署/分工方案/设计方案） | C | ✅ |

### 8.2 功能验收标准

| 功能 | 验收标准 |
|------|---------|
| ✅ 用户认证 | 新用户注册成功，登录后可访问所有功能，不同用户数据隔离 |
| ✅ 智能规划 | 输入学习目标后，AI 引导采集画像，生成结构化学习计划 |
| ✅ 计划看板 | 展示周计划卡片，点击展开每日任务，显示学习资源链接 |
| ✅ 进度追踪 | 每日打卡、难度评分、完成度统计、连续打卡天数 |
| ✅ 自适应调整 | 根据完成情况自动顺延任务、调整任务量、难度自适应 |
| ✅ 日历导出 | 下载 .ics 文件，可导入日历应用 |
| ✅ 消息持久化 | 对话消息实时保存，刷新不丢失，跨设备同步 |
| ✅ 安全防护 | 路径遍历攻击被拦截，越权访问返回 403 |

---

> **最后更新**：2026-07-23